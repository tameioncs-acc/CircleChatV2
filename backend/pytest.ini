[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
addopts = -v --cov=server --cov-report=html --cov-report=term-missing
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
